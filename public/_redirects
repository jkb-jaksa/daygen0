# Cloudflare Pages redirects configuration
# This file ensures proper handling of assets and SPA routing

# Exclude assets from SPA fallback - missing assets should return 404
# Note: Cloudflare Pages will check for actual files first before applying redirects
# So if /assets/file.js exists, it will be served; if not, it will 404 naturally

# SPA fallback for all other routes (excluding assets)
# This ensures React Router can handle client-side routing
/*    /index.html   200



